import 'package:flutter/material.dart';

// A central place for all our app's colors.
class AppColors {
  static const darkBackground = Color(0xFF070A1C);
  static const navyDark = Color(0xFF13192A);
  static const navyMid = Color(0xFF223256);

  static const gradientLeft = Color(0xFF724244);
  static const gradientRight = Color(0xFF22283B);

  static const accentYellow = Color(0xFFFDB729);
  static const accentBlue = Color(0xFF2682E0);

  static const textPrimary = Color(0xFFDCDDE9);
  static const textSecondary = Color(0xFF717584);

  static const cardDark = Color(0xFF22283B);
  // static const Color primary = Color.fromARGB(255, 186, 82, 66);
  // static const Color accent = Colors.teal;
  // static final Color background = Colors.black87;
  // static const Color textPrimary = Colors.white;
  // static final Color cardBackground = Colors.grey[900]!;
}